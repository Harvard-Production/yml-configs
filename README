This repository contains production configuration scripts.

Each version of MCC in microboone (or other software) is on it's own branch.  So, to run mcc8.6 code (as an example), checkout branch MCC8.6
